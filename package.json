{
    "name": "vitro",
    "private": true,
    "scripts": {
        "cli": "cross-env NODE_ENV=test node -r sucrase/register packages/cli/src/main.ts",
        "start": "lerna exec --parallel --ignore example* --ignore website -- tsc -w",
        "example": "yarn --cwd packages/example dev",
        "publ": "yarn lerna publish -c from-package",
        "build": "yarn build:cjs && yarn build:esm",
        "build:cjs": "lerna exec  --ignore example* --ignore website  'rm -rf dist || true && yarn tsc'",
        "build:esm": "lerna exec --parallel --scope '@vitro/ui' -- yarn tsc -m ESNext --outDir esm"
    },
    "workspaces": {
        "packages": [
            "packages/*",
            "website"
        ],
        "nohoist": [
            "**/next"
        ]
    },
    "devDependencies": {
        "lerna": "^3.22.1"
    }
}
