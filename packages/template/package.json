{
    "name": "template",
    "version": "0.0.0",
    "scripts": {
        "dev": "VITRO_TEMPLATE=1 next dev",
        "build": "yarn --cwd ../../ install && yarn --cwd ../core build && next build",
        "lock": "rm -rf node_modules && npm i --ignore-scripts && rm -rf node_modules && yarn",
        "start": "next start"
    },
    "license": "MIT",
    "dependencies": {
        "@vitro/ui": "*",
        "@vitro/plugin": "*"
    },
    "devDependencies": {
        "@types/node": "^14.0.20",
        "@types/react": "^16.9.41",
        "compose-function": "^3.0.3",
        "next-transpile-modules": "^3.3.0",
        "typescript": "3.9.6"
    }
}
